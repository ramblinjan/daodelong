# Test environment overrides — committed, no secrets here.
# Layered on top of .env.defaults when running tests.

ORGANISM_MODE=test

# Lowered limits make history-trim branches reachable in unit tests.
HEARTBEAT_MAX_HISTORY=3
BREATH_MAX_HISTORY=3
